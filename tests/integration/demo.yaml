einsum:
  declaration:
    A: [K, M]
    B: [K, N]
    Z: [M, N]
  expressions:
    - Z[m,n] = sum(K).(A[k,m] * B[k,n])
mapping:
    partitioning:
        Z:
            K: [uniform_shape(20), uniform_occupancy(A.6), uniform_occupancy(A.3)]
    loop-order:
        Z: [K3, M, K2, K1, N, K0]
